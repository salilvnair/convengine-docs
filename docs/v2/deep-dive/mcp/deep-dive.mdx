---
title: Deep Dive
sidebar_position: 5
---

import { DbTable, Highlight } from "@site/src/components/convengine";

# Deep Dive

<div className="ce-schema ce-intro-box">
The detailed MCP runtime material now lives in [Advanced Guide](/docs/v2/deep-dive/mcp/advanced).

This page is intentionally condensed so the section is not duplicated. Use it as a quick routing page:
</div>

- use [Advanced Guide](/docs/v2/deep-dive/mcp/advanced) for the current `2.0.10` execution model
- use [Semantic Query](/docs/v2/deep-dive/mcp/semantic-query) for interpret/query/postgres stage internals and code walkthrough
- use [Example 3](/docs/v2/deep-dive/mcp/example3) for the full confirmation-first planner flow
- use [Table Config](/docs/v2/deep-dive/table-config) for DB-driven semantic metadata table contracts
- use [Example 4 - Semantic Query (Zapper)](/docs/v2/deep-dive/mcp/example4) for Semantic Query in action

<DbTable
  title="Where to go now"
  columns={["Need", "Best page", "Why"]}
  rows={[
    ["Current MCP execution order", "Advanced Guide", "This now carries the condensed but complete `2.0.10` runtime model."],
    ["Semantic query internals", "Semantic Query", "Covers handlers, services, retry loop, guardrails, and stage flow."],
    ["Planner/tool confirmation flow", "Example 3", "Shows the confirmation-first multi-turn MCP branch."],
    ["Semantic metadata contracts", "Table Config", "Covers semantic metadata tables and config mapping in detail."],
    ["Real Semantic Query production-debug example", "Example 4 - Semantic Query (Zapper)", "Shows interpret -> query -> postgres runtime with transactional data."]
  ]}
/>

## What this page still means

Historically, this page held the long-form planner trace. That material was moved into Advanced Guide to avoid duplicating the same lifecycle explanation across two pages.

<Highlight type="tip" title="Recommended read path">
Read [Basics](/docs/v2/deep-dive/mcp/basics), then [Advanced Guide](/docs/v2/deep-dive/mcp/advanced), then [Semantic Query](/docs/v2/deep-dive/mcp/semantic-query), then jump into [Example 4](/docs/v2/deep-dive/mcp/example4) and [Table Config](/docs/v2/deep-dive/table-config).
</Highlight>
