---
title: Developer Guide
sidebar_position: 5
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { EngineDebugFlow, CodeBlockToggle, Highlight, DbTable } from "@site/src/components/convengine";

export const v2ExtNodes = [
  { id: "s1", data: { label: "1. LoadOrCreateConversationStep" } },
  { id: "s2", data: { label: "2. CacheInspectAuditStep" } },
  { id: "s3", data: { label: "3. ResetConversationStep" } },
  { id: "s4", data: { label: "4. PersistConversationBootstrapStep" } },
  { id: "s5", data: { label: "5. AuditUserInputStep" } },
  { id: "s6", data: { label: "6. PolicyEnforcementStep" } },
  { id: "s7", data: { label: "7. DialogueActStep" } },
  { id: "s8", data: { label: "8. InteractionPolicyStep" } },
  { id: "s9", data: { label: "9. CorrectionStep" } },
  { id: "s10", data: { label: "10. ActionLifecycleStep" } },
  { id: "s11", data: { label: "11. DisambiguationStep" } },
  { id: "s12", data: { label: "12. GuardrailStep" } },
  { id: "s13", data: { label: "13. IntentResolutionStep" } },
  { id: "s14", data: { label: "14. ResetResolvedIntentStep" } },
  { id: "s15", data: { label: "15. FallbackIntentStateStep" } },
  { id: "s16", data: { label: "16. AddContainerDataStep" } },
  { id: "s17", data: { label: "17. PendingActionStep" } },
  { id: "s18", data: { label: "18. ToolOrchestrationStep" } },
  { id: "s19", data: { label: "19. McpToolStep" } },
  { id: "s20", data: { label: "20. SchemaExtractionStep" } },
  { id: "s21", data: { label: "21. AutoAdvanceStep" } },
  { id: "s22", data: { label: "22. RulesStep" } },
  { id: "s23", data: { label: "23. StateGraphStep" } },
  { id: "s24", data: { label: "24. ResponseResolutionStep" } },
  { id: "s25", data: { label: "25. MemoryStep" } },
  { id: "s26", data: { label: "26. PersistConversationStep" } },
  { id: "s27", data: { label: "27. PipelineEndGuardStep" } },
];

export const v2ExtEdges = [
  { id: "e1", source: "s1", target: "s2" },
  { id: "e2", source: "s2", target: "s3" },
  { id: "e3", source: "s3", target: "s4" },
  { id: "e4", source: "s4", target: "s5" },
  { id: "e5", source: "s5", target: "s6" },
  { id: "e6", source: "s6", target: "s7" },
  { id: "e7", source: "s7", target: "s8" },
  { id: "e8", source: "s8", target: "s9" },
  { id: "e9", source: "s9", target: "s10" },
  { id: "e10", source: "s10", target: "s11" },
  { id: "e11", source: "s11", target: "s12" },
  { id: "e12", source: "s12", target: "s13" },
  { id: "e13", source: "s13", target: "s14" },
  { id: "e14", source: "s14", target: "s15" },
  { id: "e15", source: "s15", target: "s16" },
  { id: "e16", source: "s16", target: "s17" },
  { id: "e17", source: "s17", target: "s18" },
  { id: "e18", source: "s18", target: "s19" },
  { id: "e19", source: "s19", target: "s20" },
  { id: "e20", source: "s20", target: "s21" },
  { id: "e21", source: "s21", target: "s22" },
  { id: "e22", source: "s22", target: "s23" },
  { id: "e23", source: "s23", target: "s24" },
  { id: "e24", source: "s24", target: "s25" },
  { id: "e25", source: "s25", target: "s26" },
  { id: "e26", source: "s26", target: "s27" },
];

export const v2ExtDetails = {
  s1: { title: "LoadOrCreateConversationStep", file: "engine/steps/LoadOrCreateConversationStep.java", method: "execute(...)", stage: "BOOTSTRAP", summary: "Hydrates or creates the conversation shell.", session: ["conversation loaded"], tables: ["ce_conversation"] },
  s2: { title: "CacheInspectAuditStep", file: "engine/steps/CacheInspectAuditStep.java", method: "execute(...)", stage: "CACHE_AUDIT", summary: "Optional cache snapshot audit step.", session: ["none"], tables: ["ce_audit"] },
  s3: { title: "ResetConversationStep", file: "engine/steps/ResetConversationStep.java", method: "execute(...)", stage: "RESET", summary: "Early reset/start-over handling.", session: ["intent/state/context may reset"], tables: ["ce_conversation"] },
  s4: { title: "PersistConversationBootstrapStep", file: "engine/steps/PersistConversationBootstrapStep.java", method: "execute(...)", stage: "BOOTSTRAP_PERSIST", summary: "Ensures the conversation row exists before later writes.", session: ["bootstrap metadata"], tables: ["ce_conversation"] },
  s5: { title: "AuditUserInputStep", file: "engine/steps/AuditUserInputStep.java", method: "execute(...)", stage: "AUDIT_USER_INPUT", summary: "Persists the raw user input audit event.", session: ["none"], tables: ["ce_audit"] },
  s6: { title: "PolicyEnforcementStep", file: "engine/steps/PolicyEnforcementStep.java", method: "execute(...)", stage: "POLICY", summary: "Applies pre-intent policy controls.", session: ["payload may stop early"], tables: ["ce_policy", "ce_audit"] },
  s7: { title: "DialogueActStep", file: "engine/steps/DialogueActStep.java", method: "execute(...)", stage: "DIALOGUE_ACT", summary: "Classifies AFFIRM/NEGATE/EDIT/etc before intent resolution.", session: ["dialogue_act", "standalone_query", "resolved_user_input"], tables: ["ce_config", "ce_audit"] },
  s8: { title: "InteractionPolicyStep", file: "engine/steps/InteractionPolicyStep.java", method: "execute(...)", stage: "POLICY_ROUTING", summary: "Maps dialogue acts into deterministic routing decisions.", session: ["policy_decision", "skip_intent_resolution"], tables: ["ce_config"] },
  s9: { title: "CorrectionStep", file: "engine/steps/CorrectionStep.java", method: "execute(...)", stage: "CORRECTION", summary: "Applies in-place confirm/edit/retry routing using prompt-template interaction semantics.", session: ["routing_decision", "skip_schema_extraction", "correction_applied"], tables: ["ce_output_schema", "ce_prompt_template", "ce_audit"] },
  s10: { title: "ActionLifecycleStep", file: "engine/steps/ActionLifecycleStep.java", method: "execute(...)", stage: "PENDING_ACTION_RUNTIME", summary: "Maintains pending_action_runtime TTL and lifecycle status.", session: ["pending_action_runtime"], tables: ["ce_pending_action", "ce_audit"] },
  s11: { title: "DisambiguationStep", file: "engine/steps/DisambiguationStep.java", method: "execute(...)", stage: "DISAMBIGUATION", summary: "Pauses and asks for clarification when multiple actions fit.", session: ["pending_clarification"], tables: ["ce_pending_action", "ce_audit"] },
  s12: { title: "GuardrailStep", file: "engine/steps/GuardrailStep.java", method: "execute(...)", stage: "GUARDRAIL", summary: "Applies sanitization and approval-oriented guardrails.", session: ["guardrail flags"], tables: ["ce_audit"] },
  s13: { title: "IntentResolutionStep", file: "engine/steps/IntentResolutionStep.java", method: "execute(...)", stage: "INTENT", summary: "Resolves intent and initial state.", session: ["intent", "state"], tables: ["ce_intent", "ce_intent_classifier", "ce_audit"] },
  s14: { title: "ResetResolvedIntentStep", file: "engine/steps/ResetResolvedIntentStep.java", method: "execute(...)", stage: "RESET_INTENT", summary: "Applies reset-by-intent semantics.", session: ["intent/state may reset"], tables: ["ce_config"] },
  s15: { title: "FallbackIntentStateStep", file: "engine/steps/FallbackIntentStateStep.java", method: "execute(...)", stage: "FALLBACK", summary: "Fills missing intent/state defaults.", session: ["intent/state fallback"], tables: ["none"] },
  s16: { title: "AddContainerDataStep", file: "engine/steps/AddContainerDataStep.java", method: "execute(...)", stage: "CONTAINER_DATA", summary: "Injects container-configured data into runtime context.", session: ["container data merged"], tables: ["ce_container_config"] },
  s17: { title: "PendingActionStep", file: "engine/steps/PendingActionStep.java", method: "execute(...)", stage: "PENDING_ACTION", summary: "Executes or rejects pending actions via CeTaskExecutor.", session: ["pending action result"], tables: ["ce_pending_action", "ce_audit"] },
  s18: { title: "ToolOrchestrationStep", file: "engine/steps/ToolOrchestrationStep.java", method: "execute(...)", stage: "DIRECT_TOOL", summary: "Runs the direct request-driven tool path.", session: ["tool_result", "context.mcp.toolExecution"], tables: ["ce_mcp_tool", "ce_mcp_db_tool", "ce_audit"] },
  s19: { title: "McpToolStep", file: "engine/steps/McpToolStep.java", method: "execute(...)", stage: "MCP", summary: "Runs the planner-driven MCP loop and semantic query runtime tool chain when needed.", session: ["context.mcp.*"], tables: ["ce_mcp_tool", "ce_mcp_planner", "ce_audit"] },
  s20: { title: "SchemaExtractionStep", file: "engine/steps/SchemaExtractionStep.java", method: "execute(...)", stage: "SCHEMA", summary: "Performs structured extraction against ce_output_schema.", session: ["schema facts"], tables: ["ce_output_schema", "ce_prompt_template", "ce_audit"] },
  s21: { title: "AutoAdvanceStep", file: "engine/steps/AutoAdvanceStep.java", method: "execute(...)", stage: "AUTO_ADVANCE", summary: "Computes schema facts used for downstream transitions.", session: ["schemaComplete", "hasAny"], tables: ["none"] },
  s22: { title: "RulesStep", file: "engine/steps/RulesStep.java", method: "execute(...)", stage: "RULES", summary: "Runs phase-scoped rule evaluation and actions.", session: ["intent/state/input params may mutate"], tables: ["ce_rule", "ce_audit"] },
  s23: { title: "StateGraphStep", file: "engine/steps/StateGraphStep.java", method: "execute(...)", stage: "STATE_GRAPH", summary: "Validates legal transition paths.", session: ["state_graph_valid"], tables: ["ce_audit"] },
  s24: { title: "ResponseResolutionStep", file: "engine/steps/ResponseResolutionStep.java", method: "execute(...)", stage: "RESPONSE", summary: "Builds the final payload from ce_response + ce_prompt_template.", session: ["payload"], tables: ["ce_response", "ce_prompt_template", "ce_audit"] },
  s25: { title: "MemoryStep", file: "engine/steps/MemoryStep.java", method: "execute(...)", stage: "MEMORY", summary: "Writes memory.session_summary and optional recall memory.", session: ["memory.session_summary"], tables: ["ce_audit"] },
  s26: { title: "PersistConversationStep", file: "engine/steps/PersistConversationStep.java", method: "execute(...)", stage: "PERSIST", summary: "Persists final conversation state and result.", session: ["finalResult"], tables: ["ce_conversation"] },
  s27: { title: "PipelineEndGuardStep", file: "engine/steps/PipelineEndGuardStep.java", method: "execute(...)", stage: "END_GUARD", summary: "Final timing audit and terminal guard.", session: ["timings"], tables: ["ce_audit"] },
};

# Developer Guide

<div className="ce-schema ce-intro-box">
This page is the implementation map for consumers building on the current v2 line. It covers the real runtime pipeline as it exists now, not the smaller `2.0.0` launch-era mental model.
</div>

<EngineDebugFlow
  title="Current 27-Step Runtime Pipeline"
  subtitle="Click each step to inspect the current v2 pipeline order."
  nodes={v2ExtNodes}
  edges={v2ExtEdges}
  detailsById={v2ExtDetails}
  defaultSelectedId="s19"
/>

<Highlight type="info" title="Current repo scope">
This guide reflects the active v2 line through the later additions from `2.0.6` to `2.0.9`, including cache diagnostics, scoped MCP planner flow, `ce_verbose`, `CorrectionStep`, and Semantic Query.
</Highlight>

## Build order for a new flow

1. Define `ce_intent`, `ce_intent_classifier`, `ce_prompt_template`, `ce_response`, and `ce_rule`.
2. Add `ce_output_schema` if the flow needs structured collection or correction.
3. Add `ce_pending_action` only if the flow needs explicit confirm-before-run behavior.
4. Add `ce_mcp_tool` and `ce_mcp_planner` only after the base conversational path is deterministic.
5. Add `ce_verbose` early enough that QA and operators can see runtime progress.
6. Configure `convengine.flow.*`, `convengine.audit.*`, and the MCP namespaces in YAML.
7. Replay test and audit-trace test before production rollout.

## Version-line implementation guidance (`2.0.0` to `2.0.9`)

<DbTable
  title="What changed by version line"
  columns={["Version line", "What changed", "What developers should do differently"]}
  rows={[
    ["2.0.0", "Core table-driven engine, pending actions, tool orchestration, memory, state graph, replay foundations.", "Start with a deterministic ce_* config model instead of custom controller branching."],
    ["2.0.6", "Cache reliability and diagnostics improved.", "Treat cache health and refresh tooling as part of your production operational model."],
    ["2.0.7", "Scoped MCP planner flow, stricter tool/planner scope validation, richer MCP lifecycle metadata.", "Design tools by explicit intent/state scope and test blocked-next-tool branches."],
    ["2.0.8", "ce_verbose, stream verbose transport, step telemetry, stronger MCP schema completeness behavior.", "Seed verbose rows early and use audit + verbose together in QA."],
    ["2.0.9", "CorrectionStep, interaction semantics in prompt templates, shared Thymeleaf rendering path, Semantic Query.", "Treat prompt metadata as routing contract and keep Semantic Query logic in DB metadata, not Java if/else."]
  ]}
/>

## Review: `convengine.flow.*` features

<DbTable
  title="Flow Features"
  columns={["Subsystem", "Purpose", "Why it matters now"]}
  rows={[
    ["dialogue-act.*", "Detects AFFIRM / NEGATE / EDIT / RESET / QUESTION / NEW_REQUEST / ANSWER before intent work.", "This is the first turn-routing boundary, not a minor helper."],
    ["query-rewrite.*", "Derives standalone_query and resolved_user_input for follow-up turns.", "Important for MCP, schema extraction, and prompt rendering."],
    ["interaction-policy.*", "Maps dialogue acts into deterministic system decisions.", "This is what keeps pending-action and slot-filling paths stable."],
    ["action-lifecycle.*", "Applies TTL to pending_action_runtime.", "Prevents stale confirmations from lingering forever."],
    ["disambiguation.*", "Triggers targeted clarifications when multiple actions fit.", "Prevents accidental execution under ambiguity."],
    ["guardrail.*", "Applies sanitization and approval gating.", "This is the main pre-intent safety boundary."],
    ["state-graph.*", "Enforces legal state transitions.", "Use it before production to catch transition drift early."],
    ["memory.*", "Writes compressed memory summaries.", "Useful when long conversations would otherwise bloat context."],
    ["tool-orchestration.*", "Controls the direct one-shot tool path.", "Keeps direct tool execution separate from planner MCP behavior."]
  ]}
/>

## Extension points

<Tabs groupId="extensions">
  <TabItem value="tasks" label="CeTask" default>

<CodeBlockToggle title="Creating a Pending Action Task" language="java" packagePath="com.zapper.convengine.tasks">

```java
@Component("shippingConfirmationTask")
public class ShippingConfirmationTask implements CeTask {
  @Override
  public TaskResult execute(EngineSession session, CePendingAction action) {
    String trackingId = (String) session.getInputParams().get("trackingId");
    session.putInputParam("shippingStatus", "DISPATCHED");
    return TaskResult.success();
  }
}
```

</CodeBlockToggle>

  </TabItem>
  <TabItem value="tools" label="McpToolExecutor">

<CodeBlockToggle title="Tool execution adapter" language="java" packagePath="com.zapper.convengine.tools">

```java
@Component("billingToolAdapter")
public class BillingToolAdapter implements McpToolExecutor {
  @Override
  public ToolExecutionResult invoke(ToolRequest request, EngineSession session) {
    if ("BILLING".equals(request.getToolGroup())) {
      return ToolExecutionResult.success(Map.of("balance", 0.00));
    }
    return ToolExecutionResult.skipped();
  }
}
```

</CodeBlockToggle>

  </TabItem>
  <TabItem value="memory" label="Memory Store">

<CodeBlockToggle title="Custom Session Snapshotting" language="java" packagePath="com.zapper.convengine.stores">

```java
@Component
public class CustomMemoryStore implements ConversationMemoryStore {
  @Override
  public String summarize(List<TurnSnapshot> turns) {
    return llmClient.summarizeWithLimits(turns, 100);
  }
}
```

</CodeBlockToggle>

  </TabItem>
</Tabs>

## Semantic Query implementation guidance

If you are using Semantic Query, treat it as canonical-intent + metadata-driven SQL generation, not a free-form SQL generator.

Core runtime path:

1. `DbSemanticInterpretToolHandler`
2. `SemanticInterpretService`
3. `DbSemanticQueryToolHandler`
4. `SemanticLlmQueryService`
5. `PostgresQueryToolHandler`
6. `SemanticFailureFeedbackService` (failure/correction memory)

Implementation rules:

- keep business semantics in `ce_semantic_concept` + `ce_semantic_synonym`
- keep field/table mapping in `ce_semantic_mapping` + `ce_semantic_query_class`
- keep ambiguity handling in `ce_semantic_ambiguity_option`
- keep vector retrieval corpus in `ce_semantic_concept_embedding`
- keep retry memory and corrected SQL in `ce_semantic_query_failures`
- keep Java generic; avoid project-specific query logic hardcoded in handlers

Related Semantic Query docs:

- [MCP](/docs/v2/deep-dive/mcp)
- [MCP Semantic Query](/docs/v2/deep-dive/mcp/semantic-query)
- [Table Config](/docs/v2/deep-dive/table-config)
- [MCP Example 4 - Zapper Disconnect](/docs/v2/deep-dive/mcp/example4)

## Operational safety checklist

- **Fallbacks**: Every reachable state should still resolve a valid `ce_response` path.
- **Phase Targeting**: Split state changes across the actual runtime phases instead of overloading one rule row.
- **Prompt Contracts**: Define `interaction_mode` and `interaction_contract` intentionally so `CorrectionStep` behaves predictably.
- **Scope Discipline**: Keep `ce_mcp_tool` and `ce_mcp_planner` scoped by real intent/state whenever possible.
- **Guardrails**: Monitor `ce_audit` for guardrail and MCP blocked branches.
- **Semantic discipline**: If Semantic Query is enabled, validate allowlists/mappings and test unsupported + clarification paths before rollout.
- **State Graph**: Enable strict transition validation before promoting a new flow to production.

## Regression strategy

Use `ConversationReplayService` plus audit trace inspection to validate not just the final answer, but the path the engine took.

<CodeBlockToggle title="ConversationReplayService assertion" language="java" filePath="src/test/java/com/zapper/RegressionTests.java">

```java
@Test
public void testShippingFlow() {
  ReplayResult finalTurn = replayService.runScript("shipping_confirmation_script.json");

  assertEquals("SHIPPING_CONFIRMED", finalTurn.getFinalIntent());
  assertEquals("EXECUTE_PENDING_ACTION", finalTurn.getAuditStages().getLastSystemAction());
}
```

</CodeBlockToggle>
